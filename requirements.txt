# Python Dependencies for EV-seq Analysis Workflow
# Version: 1.0.0
# Last updated: December 2025

# Core scientific computing libraries
numpy==1.21.6
pandas==1.3.5
scipy==1.7.3

# Web requests for SGD lookup
requests==2.28.2

# Note: Standard library modules used (no versions needed):
# - os, sys, subprocess, configparser, argparse, glob
# - These are part of Python standard library

# External tools required (install separately):
# - samtools (for BAM file processing)
# - BWA-MEM (for alignment) 
# - BEDtools (for coverage calculation)
# - FastQC (for quality control)
# - R with LOLA package (for enrichment analysis)
#   Install R packages: Rscript -e "install.packages(c('LOLA', 'GenomicRanges', 'data.table'))"